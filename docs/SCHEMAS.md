# SCHEMAS.md

Last Updated: 2026-05-07
Source of truth: `db/schema.ts`. This document mirrors it for review.

## Tables

### users

Mirror of Supabase `auth.users`. Kept locally for FK joins.

| column | type | notes |
|---|---|---|
| id | uuid PK | matches Supabase auth user id |
| email | text not null | |
| display_name | text | |
| created_at | timestamptz not null default now() | |

### organizations

Tenant root. Not org-scoped itself.

| column | type | notes |
|---|---|---|
| id | uuid PK default random | |
| slug | text not null unique | |
| name | text not null | |
| created_at | timestamptz not null default now() | |

### memberships

`(org_id, user_id)` is unique. One membership row per user-org pair.

| column | type | notes |
|---|---|---|
| id | uuid PK | |
| org_id | uuid not null FK organizations(id) ON DELETE CASCADE | |
| user_id | uuid not null FK users(id) ON DELETE CASCADE | |
| role | enum admin/member/viewer not null | |
| created_at | timestamptz | |

Indexes: `(org_id, user_id)` UNIQUE, `(user_id)`.

### notes (TENANT-OWNED)

| column | type | notes |
|---|---|---|
| id | uuid PK | |
| org_id | uuid not null FK organizations(id) | tenant scope |
| author_id | uuid not null FK users(id) | |
| title | text not null | |
| content | text not null default '' | |
| visibility | enum private/org/shared not null default 'org' | |
| tags_text | text not null default '' | denormalized for FTS (ADR-0004) |
| created_at | timestamptz | |
| updated_at | timestamptz | |
| deleted_at | timestamptz | soft-delete |

Composite indexes (lead with `org_id` per ADR-0001):
- `(org_id, updated_at)`
- `(org_id, author_id)`
- `(org_id, visibility)`

### note_versions (TENANT-OWNED)

Append-only history.

| column | type | notes |
|---|---|---|
| id | uuid PK | |
| org_id | uuid not null | tenant scope |
| note_id | uuid not null FK notes(id) ON DELETE CASCADE | |
| version | int not null | monotonic per (org, note) |
| title | text not null | |
| content | text not null | |
| author_id | uuid not null | |
| created_at | timestamptz | |

Indexes: `(org_id, note_id, version)` UNIQUE, `(org_id, note_id, created_at)`.

### tags (TENANT-OWNED)

| column | type | notes |
|---|---|---|
| id | uuid PK | |
| org_id | uuid not null | tenant scope |
| name | text not null | |
| created_at | timestamptz | |

Indexes: `(org_id, name)` UNIQUE.

### note_tags (TENANT-OWNED)

Join table. Composite primary key `(org_id, note_id, tag_id)`.

| column | type | notes |
|---|---|---|
| org_id | uuid not null | |
| note_id | uuid not null FK notes(id) ON DELETE CASCADE | |
| tag_id | uuid not null FK tags(id) ON DELETE CASCADE | |

Indexes: PK + `(org_id, tag_id)`.

### files (TENANT-OWNED)

| column | type | notes |
|---|---|---|
| id | uuid PK | |
| org_id | uuid not null | tenant scope |
| note_id | uuid FK notes(id) ON DELETE SET NULL | optional parent |
| uploader_id | uuid not null FK users(id) | |
| storage_path | text not null UNIQUE | `org/<org_id>/note/<note_id>/<file_id>-<filename>` |
| filename | text not null | |
| mime_type | text not null | |
| size_bytes | bigint not null | |
| created_at | timestamptz | |
| deleted_at | timestamptz | soft-delete |

Indexes: `(org_id, note_id)`, `(org_id, created_at)`, `(storage_path)` UNIQUE.

### audit_log (TENANT-OWNED)

Append-only durable audit trail.

| column | type | notes |
|---|---|---|
| id | uuid PK | |
| org_id | uuid not null | tenant scope |
| actor_id | uuid FK users(id) ON DELETE SET NULL | nullable for system events |
| event | text not null | matches `LOG_EVENTS` taxonomy |
| entity_type | text | e.g. 'note', 'file' |
| entity_id | text | |
| payload | jsonb not null default '{}' | redacted fields per `logging/redact.ts` |
| success | bool not null default true | |
| created_at | timestamptz | |

Indexes: `(org_id, created_at)`, `(org_id, event)`.

## Migrations

Generated by drizzle-kit into `drizzle/`. Reproducible via `npm run db:generate`. Apply with `npm run db:migrate`.
